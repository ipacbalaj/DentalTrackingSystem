<UserControl x:Class="ClientShell.Views.Tiles.PatientTileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Border Background="PaleTurquoise" MinWidth="200" MaxWidth="250" BorderBrush="Black" BorderThickness="0 0 0 0.5">
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        <TextBlock FontWeight="SemiBold" Text="{Binding Name}"/>
        <TextBlock FontWeight="SemiBold" Grid.Column="1" Text="{Binding Surname}"/>
    </Grid>
    </Border>
</UserControl>
