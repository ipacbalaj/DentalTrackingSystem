<UserControl x:Class="DSA.Module.DentalRecords.Options.UploadInterventionsFIle.UploadInterventionsFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:buttons="clr-namespace:DSA.Common.Controls.Buttons;assembly=DSA.Common.Controls"
             xmlns:symbolButton="clr-namespace:DSA.Common.Controls.Buttons.SymbolButton;assembly=DSA.Common.Controls"
             mc:Ignorable="d" 
            >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Path}" VerticalAlignment="Center" VerticalContentAlignment="Center" ></TextBox>
        <symbolButton:SymbolIconButtonView Grid.Row="1" DataContext="{Binding UploadPatientsButton}"/>
        <symbolButton:SymbolIconButtonView Grid.Row="2" DataContext="{Binding UploadSettingsButton}"/>
        <symbolButton:SymbolIconButtonView Grid.Row="2" DataContext="{Binding UploadMonthIntervention}"/>
    </Grid>
</UserControl>
