<UserControl x:Class="DSA.Common.Controls.Buttons.SymbolButton.SymbolIconButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="30">
    <Grid>
        <Button Command="{Binding ButtonCommand}" MouseEnter="UIElement_OnMouseEnter" MouseLeave="UIElement_OnMouseLeave" VerticalAlignment="Center">
            <TextBlock VerticalAlignment="Center" Text="{Binding Symbol}" Background="Transparent"  FontSize="14" Foreground="{Binding SymbolColor}"/>
        </Button>
    </Grid>
</UserControl>
