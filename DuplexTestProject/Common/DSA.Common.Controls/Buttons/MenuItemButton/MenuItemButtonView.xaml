<UserControl x:Class="DSA.Common.Controls.Buttons.MenuItemButton.MenuItemButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="MenuButton"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border MinHeight="40" BorderBrush="{Binding BorderBackground}" Margin="-5 0 0 0" BorderThickness="1.5" Background="{Binding ButtonBackground}"
                MouseUp="OnMouseUp" MouseEnter="OnMouseEnter" MouseLeave="OnMouseLeave"
                 >
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="15 5 15 5">
                <Run
                       Text="{Binding Text}" Foreground="{Binding TextColor}" 
                       FontWeight="UltraBlack" FontSize="20" FontFamily="Tahoma"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
